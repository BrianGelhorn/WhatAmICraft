---
description: Diseñar y mejorar escenas, minibiomas y familias visuales Minecraft; implementar pruebas cuando se solicite.
---

Usa la skill minecraft-clip-director para esta solicitud:

$ARGUMENTS

Si no hay solicitud, pregunta qué escena, minibioma o familia quiere trabajar. Para escenas usa `resources/minibiomes.md`. Elige solo las rutas explícitas: diseñar, revisar, blockout, detallar, implementar o validar. Una petición de escena no autoriza familias, episodios, candidatos, lore ni contexto adicional. `diseña` y `revisa` no editan Server; `genera`, `implementa`, `instala` o `prueba en Docker` sí autorizan esa implementación acotada. No confundas un decorado con una familia validada ni declares calidad visual sin capturas. Usa el modelo seleccionado, sin cambiar configuración ni proveedor.
